#include <stdio.h>
int main()
{
	// вар 22
	int N = 10; // переменная для кол-ва элементов в массиве
	int massive[N];
	int d1, d2;
	d1 = 5;
	d2 = 20;
	for(int i = 0; i <= N; i++){ // номер числа массива
	 for(int k = d1; k <= d2; k++) // число из диапозона
	  for(int l = k; l < k; l++){   // проверка числа на простоту 
		float rem = l % k;
		if(rem == 0){
		   tru
			}
		
		}
	}
	
}
